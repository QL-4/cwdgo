# 06 — 快捷键配置

**What to build:** 设置中配置 Launcher Hotkey：预置选项 `Alt+X`（默认）与 `Win+Q`，也支持自定义组合键录制；保存后热键即时重新注册（旧热键失效、新热键可用），重启后保留；面板开关交互不变。

**Blocked by:** 05（设置窗口）

**Status:** ready-for-agent

- [ ] 设置中可切换 `Alt+X` / `Win+Q` / 自定义组合键
- [ ] 保存后旧热键立即失效、新热键立即可用（无需重启）
- [ ] 自定义组合录制交互清晰（等待按键、冲突提示）
- [ ] 所选热键重启后保留
- [ ] 注册冲突（如被系统占用）时给出可读提示，且不丢失原热键
